# Foundry
This is a Web App that is designed to store SVG's in a place, so the user can easily navigate trough them. The SVG also contain a button to Copy code, so the user can easily copy and use it. 
